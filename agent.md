# 网络加密流量二进制分析：Agent 工作规范

## 三人验收协作入口

执行验收冲刺的Codex任务先读 `docs/acceptance/codex-execution-contract.md`，再按用户指定的A/B/C读取 `docs/acceptance/role-a.md`、`role-b.md` 或 `role-c.md`。角色入口定义最小阅读集、写入范围和实际交接要求；总验收标准见 `docs/plans/2026-09-10-1827-feat-acceptance-closure-plan.md`。未指定角色时不能自行占用三个角色的文件。仅要求检查或修改计划的任务不自动开始开发。

## 当前任务

M1～M12 的模块调研与初步选型已经完成。用户已于 2026-09-08 明确要求进入开发阶段；当前任务是按可验证的小步迭代实现、测试并集成完整分析链路。

调研推荐仍不代表已验证可用。工程状态以 `research/progress.md` 和实际测试结果为准；历史运行结果必须与当前环境复测结果分开表述。

## 工作流程

1. 查看 `research/progress.md`、已有实现与测试，先复现当前基线，再修改。
2. 默认按 M1 → M12 推进；前置条件缺失时使用带真值 fixture 验证独立能力，并明确尚未贯通的部分。
3. 每个模块至少提供可调用入口、结构化输出、正常与边界测试、依赖/重跑说明；不得伪造运行结果。
4. 保留输入、参数、半开区间偏移、缺失信息和证据链。更新进度后再进入下一模块。
5. 新增外部依赖、数据或模型前先证明必要性并记录版本、来源、许可与哈希。

候选的用途、特点和主要限制已有可靠来源，能够支持初步比较，即可结束该模块调研。无需运行结果、评分表或验收等级。

## 模块范围与关注点

以下名称仅为检索线索，不表示已经核实或确定采用。

| 模块 | 功能 | 检索方向与需要说明的特点 |
|---|---|---|
| M1 | 数据输入与预处理 | 标准库、TShark、PyShark、Scapy、dpkt；DAT/抓包输入区别、TCP 重组、方向与元数据支持 |
| M2 | 二进制基础特征 | 标准库、NumPy；长度、字节频率、熵、ASCII/零字节比例、重复模式 |
| M3 | 消息边界识别 | Netzob、Magic/长度字段/分隔符/固定长度方法；能否处理连续流，是否要求已切分消息 |
| M4 | 消息聚类 | Netzob、DBSCAN、K-Medoids；特征与距离、变长消息、噪声处理、是否需要簇数 |
| M5 | 消息对齐 | Netzob、Needleman–Wunsch；双序列/多序列能力、二进制字节支持、gap 与变长处理 |
| M6 | 字段边界与格式推断 | 统计 PRE、Netzob、经核实的相关论文；分帧/聚类/对齐依赖、变长字段、训练和代码可用性 |
| M7 | 标准协议识别与解析 | Wireshark/TShark；HTTP、SMTP、POP3、IMAP、FTP、TLS、SSH、DNS 覆盖与识别条件 |
| M8 | 明文提取与数据恢复 | 文本、Hex、Base64、gzip/zlib；支持的编码/压缩、恢复条件和加密数据限制 |
| M9 | 流量统计特征 | CICFlowMeter、轻量统计方法；包长、方向、时间间隔、流持续时间、突发及元数据要求 |
| M10 | 访问行为分析 | 规则与统计特征；周期通信、上下行主导、突发和长连接等行为描述及解释依据 |
| M11 | 行为类型分类 | Random Forest、XGBoost、SVM、流量分类论文；标签、数据集、训练需求和适用类别 |
| M12 | LLM 语义分析与报告 | 结构化证据输入、带引用的解释、模板报告；模型作用、接入条件及推断局限 |

## 当前交付

按实际开发进度维护以下内容，不提前生成空模块或伪日志：

- `research/progress.md`：每模块状态及必要的下一步。
- `research/Mxx-名称/report.md`：候选特点与初步建议，来源直接附在对应内容旁。
- `research/selection.md`：各模块的推荐、主要特点和限制。
- `experiments/Mxx/`：可运行入口、测试、模块说明及必要的真实运行证据。
- 根 `README.md`：当前能力、重跑方式、限制与里程碑。

单模块报告格式：

```markdown
# Mx 模块名称
状态：待调研 / 调研中 / 已调研

用途：一句话说明。

| 候选 | 主要特点与优势 | 输入输出及前置条件 | 限制 | 来源 |
|---|---|---|---|---|

初步建议：推荐什么、为什么，以及待核实内容。
```

“已调研”仅表示检索和特点比较完成，不表示已测试或集成成功。无需分别生成检索日志、候选卡、评分表、实验报告、架构图或总报告；有用内容合并到模块报告即可。

## 执行边界

- 可以实现适配器、构造可追溯 fixture 并执行测试；优先标准库和已存在依赖。
- 未经用户授权不调用付费 API、不外传真实数据、不训练高成本模型、不下载大型数据或权重。
- 缺少真实 DAT、Wireshark、GPU、标签或模型凭据时，继续完成不依赖它们的模块，并把相关能力标为未验证或阻塞。
- 不要求先建分支、提交、创建 PR 或安排多 Agent。
- 不编造来源、论文、运行结果或性能。论文声称的效果须标明来源，不能写成本地实测。未核实的代码归属和能力明确标注。
- `.dat` 扩展名不能确定数据格式；缺少时间戳、方向或网络头时，不假定存在 Flow 元数据。
- 高熵不能直接证明加密；不承诺无密钥恢复 TLS/AES 明文；LLM 解释不能替代协议解析证据。
- 中断后从当前调研进度继续，不自动恢复旧实验待办。保留已有代码、数据和真实记录；过时流程及空模板归档，不作为当前要求。
